#include <iostream>

int main()
{
    // The const keyword specifies that a variabe's value is constant
    // tells the compiler to prevent antthing from modifying it
    // (read only)
    return 0;
}